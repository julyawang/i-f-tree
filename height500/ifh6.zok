def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, field a7) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 477 then (if a2 < 442 then (if a3 < 419 then (if a4 < 493 then (if a5 < 460 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a7 then 1 else 0 fi
    return verify
