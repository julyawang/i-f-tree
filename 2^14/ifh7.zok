def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, field a8) -> (field): 
    field res = (if a0 >= 11610 then (if a1 < 15458 then (if a2 < 14030 then (if a3 < 14454 then (if a4 < 13006 then (if a5 < 11743 then (if a6 < 9752 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a8 then 1 else 0 fi
    return verify
