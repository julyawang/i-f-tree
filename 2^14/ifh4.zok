def main(private field a0, private field a1, private field a2, private field a3, field a5) -> (field): 
    field res = (if a0 >= 11610 then (if a1 < 11467 then (if a2 < 10904 then (if a3 < 15257 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a5 then 1 else 0 fi
    return verify
