def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, field a9) -> (field): 
    field res = (if a0 >= 11610 then (if a1 < 13484 then (if a2 < 10726 then (if a3 < 13930 then (if a4 < 10134 then (if a5 < 13913 then (if a6 < 8239 then (if a7 < 14222 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a9 then 1 else 0 fi
    return verify
