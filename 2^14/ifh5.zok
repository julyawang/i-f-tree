def main(private field a0, private field a1, private field a2, private field a3, private field a4, field a6) -> (field): 
    field res = (if a0 >= 11610 then (if a1 < 13770 then (if a2 < 16334 then (if a3 < 10279 then (if a4 < 13385 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a6 then 1 else 0 fi
    return verify
