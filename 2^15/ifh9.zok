def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, field a10) -> (field): 
    field res = (if a0 >= 25458 then (if a1 < 31595 then (if a2 < 23330 then (if a3 < 29172 then (if a4 < 19388 then (if a5 < 25853 then (if a6 < 32664 then (if a7 < 32377 then (if a8 < 29060 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a10 then 1 else 0 fi
    return verify
