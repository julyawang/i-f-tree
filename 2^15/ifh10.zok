def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, field a11) -> (field): 
    field res = (if a0 >= 25458 then (if a1 < 32328 then (if a2 < 22915 then (if a3 < 28026 then (if a4 < 28256 then (if a5 < 24852 then (if a6 < 31692 then (if a7 < 29995 then (if a8 < 20624 then (if a9 < 17698 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a11 then 1 else 0 fi
    return verify
