def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, field a8) -> (field): 
    field res = (if a0 >= 25458 then (if a1 < 16879 then (if a2 < 30572 then (if a3 < 27063 then (if a4 < 32118 then (if a5 < 20850 then (if a6 < 18135 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a8 then 1 else 0 fi
    return verify
