def main(private field a0, private field a1, private field a2) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 173 then 1 else 0 fi) + (if a2 >= 171 then 1 else 0 fi)
    return res