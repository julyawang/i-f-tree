def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, field a13) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 57 then (if a2 < 24 then (if a3 < 35 then (if a4 < 75 then (if a5 < 83 then (if a6 < 0 then (if a7 < 3 then (if a8 < 61 then (if a9 < 33 then (if a10 < 35 then (if a11 < 68 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a13 then 1 else 0 fi
    return verify
