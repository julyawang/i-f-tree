def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, private field a15, private field a16, private field a17, private field a18, private field a19, private field a20, private field a21, private field a22, private field a23, field a25) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 194 then (if a2 < 175 then (if a3 < 115 then (if a4 < 179 then (if a5 < 165 then (if a6 < 131 then (if a7 < 157 then (if a8 < 187 then (if a9 < 184 then (if a10 < 177 then (if a11 < 118 then (if a12 < 120 then (if a13 < 110 then (if a14 < 146 then (if a15 < 199 then (if a16 < 118 then (if a17 < 133 then (if a18 < 102 then (if a19 < 141 then (if a20 < 124 then (if a21 < 120 then (if a22 < 168 then (if a23 < 121 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a25 then 1 else 0 fi
    return verify
