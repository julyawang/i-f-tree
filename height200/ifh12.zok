def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, field a13) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 168 then (if a2 < 163 then (if a3 < 147 then (if a4 < 175 then (if a5 < 120 then (if a6 < 187 then (if a7 < 105 then (if a8 < 150 then (if a9 < 152 then (if a10 < 139 then (if a11 < 117 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a13 then 1 else 0 fi
    return verify
