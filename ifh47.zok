def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, private field a15, private field a16, private field a17, private field a18, private field a19, private field a20, private field a21, private field a22, private field a23, private field a24, private field a25, private field a26, private field a27, private field a28, private field a29, private field a30, private field a31, private field a32, private field a33, private field a34, private field a35, private field a36, private field a37, private field a38, private field a39, private field a40, private field a41, private field a42, private field a43, private field a44, private field a45, private field a46, field a48) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 93 then (if a2 < 38 then (if a3 < 27 then (if a4 < 72 then (if a5 < 60 then (if a6 < 33 then (if a7 < 7 then (if a8 < 68 then (if a9 < 84 then (if a10 < 86 then (if a11 < 11 then (if a12 < 36 then (if a13 < 17 then (if a14 < 25 then (if a15 < 98 then (if a16 < 2 then (if a17 < 58 then (if a18 < 97 then (if a19 < 89 then (if a20 < 11 then (if a21 < 1 then (if a22 < 87 then (if a23 < 92 then (if a24 < 46 then (if a25 < 86 then (if a26 < 33 then (if a27 < 92 then (if a28 < 31 then (if a29 < 64 then (if a30 < 74 then (if a31 < 86 then (if a32 < 23 then (if a33 < 24 then (if a34 < 6 then (if a35 < 90 then (if a36 < 19 then (if a37 < 89 then (if a38 < 11 then (if a39 < 67 then (if a40 < 3 then (if a41 < 33 then (if a42 < 77 then (if a43 < 10 then (if a44 < 90 then (if a45 < 13 then (if a46 < 22 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a48 then 1 else 0 fi
    return verify
