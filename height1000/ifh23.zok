def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, private field a15, private field a16, private field a17, private field a18, private field a19, private field a20, private field a21, private field a22, field a24) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 974 then (if a2 < 960 then (if a3 < 927 then (if a4 < 944 then (if a5 < 999 then (if a6 < 957 then (if a7 < 960 then (if a8 < 920 then (if a9 < 964 then (if a10 < 900 then (if a11 < 981 then (if a12 < 927 then (if a13 < 923 then (if a14 < 994 then (if a15 < 945 then (if a16 < 940 then (if a17 < 900 then (if a18 < 973 then (if a19 < 995 then (if a20 < 967 then (if a21 < 905 then (if a22 < 915 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a24 then 1 else 0 fi
    return verify
