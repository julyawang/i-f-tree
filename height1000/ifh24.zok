def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, private field a15, private field a16, private field a17, private field a18, private field a19, private field a20, private field a21, private field a22, private field a23, field a25) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 954 then (if a2 < 965 then (if a3 < 976 then (if a4 < 919 then (if a5 < 965 then (if a6 < 999 then (if a7 < 993 then (if a8 < 933 then (if a9 < 932 then (if a10 < 905 then (if a11 < 940 then (if a12 < 919 then (if a13 < 971 then (if a14 < 914 then (if a15 < 914 then (if a16 < 900 then (if a17 < 980 then (if a18 < 904 then (if a19 < 952 then (if a20 < 918 then (if a21 < 946 then (if a22 < 903 then (if a23 < 957 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a25 then 1 else 0 fi
    return verify
