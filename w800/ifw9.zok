def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 781 then 1 else 0 fi) + (if a2 >= 796 then 1 else 0 fi) + (if a3 >= 754 then 1 else 0 fi) + (if a4 >= 765 then 1 else 0 fi) + (if a5 >= 716 then 1 else 0 fi) + (if a6 >= 719 then 1 else 0 fi) + (if a7 >= 736 then 1 else 0 fi) + (if a8 >= 782 then 1 else 0 fi)
    return res