def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 714 then 1 else 0 fi) + (if a2 >= 700 then 1 else 0 fi) + (if a3 >= 785 then 1 else 0 fi) + (if a4 >= 798 then 1 else 0 fi) + (if a5 >= 748 then 1 else 0 fi) + (if a6 >= 737 then 1 else 0 fi) + (if a7 >= 792 then 1 else 0 fi) + (if a8 >= 743 then 1 else 0 fi) + (if a9 >= 723 then 1 else 0 fi)
    return res