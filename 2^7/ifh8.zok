def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, field a9) -> (field): 
    field res = (if a0 >= 90 then (if a1 < 65 then (if a2 < 124 then (if a3 < 78 then (if a4 < 69 then (if a5 < 108 then (if a6 < 108 then (if a7 < 90 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a9 then 1 else 0 fi
    return verify
