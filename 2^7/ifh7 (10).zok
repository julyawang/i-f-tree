def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, field a11) -> (field): 
    field res = (if a0 >= 90 then (if a1 < 73 then (if a2 < 79 then (if a3 < 125 then (if a4 < 73 then (if a5 < 64 then (if a6 < 118 then (if a7 < 90 then (if a8 < 92 then (if a9 < 68 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a11 then 1 else 0 fi
    return verify
