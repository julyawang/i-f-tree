def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 881 then 1 else 0 fi) + (if a2 >= 807 then 1 else 0 fi) + (if a3 >= 845 then 1 else 0 fi) + (if a4 >= 872 then 1 else 0 fi) + (if a5 >= 896 then 1 else 0 fi) + (if a6 >= 890 then 1 else 0 fi) + (if a7 >= 814 then 1 else 0 fi) + (if a8 >= 867 then 1 else 0 fi)
    return res