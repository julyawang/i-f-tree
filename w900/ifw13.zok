def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 817 then 1 else 0 fi) + (if a2 >= 841 then 1 else 0 fi) + (if a3 >= 804 then 1 else 0 fi) + (if a4 >= 879 then 1 else 0 fi) + (if a5 >= 883 then 1 else 0 fi) + (if a6 >= 889 then 1 else 0 fi) + (if a7 >= 896 then 1 else 0 fi) + (if a8 >= 885 then 1 else 0 fi) + (if a9 >= 886 then 1 else 0 fi) + (if a10 >= 857 then 1 else 0 fi) + (if a11 >= 866 then 1 else 0 fi) + (if a12 >= 848 then 1 else 0 fi)
    return res