def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 855 then 1 else 0 fi) + (if a2 >= 812 then 1 else 0 fi) + (if a3 >= 857 then 1 else 0 fi) + (if a4 >= 828 then 1 else 0 fi) + (if a5 >= 864 then 1 else 0 fi) + (if a6 >= 828 then 1 else 0 fi) + (if a7 >= 883 then 1 else 0 fi) + (if a8 >= 841 then 1 else 0 fi) + (if a9 >= 867 then 1 else 0 fi) + (if a10 >= 816 then 1 else 0 fi) + (if a11 >= 886 then 1 else 0 fi)
    return res