def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 859 then 1 else 0 fi) + (if a2 >= 858 then 1 else 0 fi) + (if a3 >= 864 then 1 else 0 fi) + (if a4 >= 800 then 1 else 0 fi) + (if a5 >= 807 then 1 else 0 fi) + (if a6 >= 875 then 1 else 0 fi) + (if a7 >= 897 then 1 else 0 fi) + (if a8 >= 804 then 1 else 0 fi) + (if a9 >= 882 then 1 else 0 fi)
    return res