def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, field a10) -> (field): 
    field res = (if a0 >= 168 then (if a1 < 231 then (if a2 < 183 then (if a3 < 238 then (if a4 < 240 then (if a5 < 208 then (if a6 < 148 then (if a7 < 175 then (if a8 < 216 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a10 then 1 else 0 fi
    return verify
