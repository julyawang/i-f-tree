def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, field a8) -> (field): 
    field res = (if a0 >= 168 then (if a1 < 186 then (if a2 < 185 then (if a3 < 182 then (if a4 < 210 then (if a5 < 246 then (if a6 < 197 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a8 then 1 else 0 fi
    return verify
