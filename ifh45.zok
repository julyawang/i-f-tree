def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, private field a15, private field a16, private field a17, private field a18, private field a19, private field a20, private field a21, private field a22, private field a23, private field a24, private field a25, private field a26, private field a27, private field a28, private field a29, private field a30, private field a31, private field a32, private field a33, private field a34, private field a35, private field a36, private field a37, private field a38, private field a39, private field a40, private field a41, private field a42, private field a43, private field a44, field a46) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 34 then (if a2 < 65 then (if a3 < 73 then (if a4 < 47 then (if a5 < 89 then (if a6 < 72 then (if a7 < 57 then (if a8 < 65 then (if a9 < 69 then (if a10 < 52 then (if a11 < 2 then (if a12 < 77 then (if a13 < 0 then (if a14 < 64 then (if a15 < 97 then (if a16 < 67 then (if a17 < 53 then (if a18 < 24 then (if a19 < 59 then (if a20 < 40 then (if a21 < 58 then (if a22 < 98 then (if a23 < 26 then (if a24 < 95 then (if a25 < 97 then (if a26 < 83 then (if a27 < 41 then (if a28 < 7 then (if a29 < 37 then (if a30 < 36 then (if a31 < 65 then (if a32 < 72 then (if a33 < 41 then (if a34 < 61 then (if a35 < 58 then (if a36 < 94 then (if a37 < 40 then (if a38 < 84 then (if a39 < 57 then (if a40 < 63 then (if a41 < 12 then (if a42 < 49 then (if a43 < 46 then (if a44 < 47 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a46 then 1 else 0 fi
    return verify
