def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 244 then 1 else 0 fi) + (if a2 >= 297 then 1 else 0 fi) + (if a3 >= 203 then 1 else 0 fi) + (if a4 >= 272 then 1 else 0 fi) + (if a5 >= 233 then 1 else 0 fi) + (if a6 >= 204 then 1 else 0 fi) + (if a7 >= 237 then 1 else 0 fi) + (if a8 >= 225 then 1 else 0 fi) + (if a9 >= 253 then 1 else 0 fi) + (if a10 >= 292 then 1 else 0 fi) + (if a11 >= 283 then 1 else 0 fi)
    return res