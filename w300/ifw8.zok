def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 251 then 1 else 0 fi) + (if a2 >= 249 then 1 else 0 fi) + (if a3 >= 250 then 1 else 0 fi) + (if a4 >= 279 then 1 else 0 fi) + (if a5 >= 297 then 1 else 0 fi) + (if a6 >= 278 then 1 else 0 fi) + (if a7 >= 275 then 1 else 0 fi)
    return res