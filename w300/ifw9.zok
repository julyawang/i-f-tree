def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 255 then 1 else 0 fi) + (if a2 >= 271 then 1 else 0 fi) + (if a3 >= 219 then 1 else 0 fi) + (if a4 >= 271 then 1 else 0 fi) + (if a5 >= 228 then 1 else 0 fi) + (if a6 >= 212 then 1 else 0 fi) + (if a7 >= 220 then 1 else 0 fi) + (if a8 >= 233 then 1 else 0 fi)
    return res