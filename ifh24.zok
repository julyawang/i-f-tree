def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, private field a15, private field a16, private field a17, private field a18, private field a19, private field a20, private field a21, private field a22, private field a23, field a25) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 58 then (if a2 < 98 then (if a3 < 56 then (if a4 < 45 then (if a5 < 67 then (if a6 < 51 then (if a7 < 59 then (if a8 < 57 then (if a9 < 42 then (if a10 < 74 then (if a11 < 70 then (if a12 < 8 then (if a13 < 4 then (if a14 < 30 then (if a15 < 16 then (if a16 < 18 then (if a17 < 38 then (if a18 < 17 then (if a19 < 26 then (if a20 < 19 then (if a21 < 94 then (if a22 < 14 then (if a23 < 35 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a25 then 1 else 0 fi
    return verify
