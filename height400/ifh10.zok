def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, field a11) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 317 then (if a2 < 329 then (if a3 < 315 then (if a4 < 399 then (if a5 < 370 then (if a6 < 379 then (if a7 < 352 then (if a8 < 306 then (if a9 < 348 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a11 then 1 else 0 fi
    return verify
