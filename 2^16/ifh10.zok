def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, field a11) -> (field): 
    field res = (if a0 >= 61021 then (if a1 < 51027 then (if a2 < 56114 then (if a3 < 52256 then (if a4 < 48490 then (if a5 < 33612 then (if a6 < 58263 then (if a7 < 65039 then (if a8 < 50991 then (if a9 < 39669 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a11 then 1 else 0 fi
    return verify
