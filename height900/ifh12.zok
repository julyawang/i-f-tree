def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, field a13) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 821 then (if a2 < 844 then (if a3 < 875 then (if a4 < 864 then (if a5 < 830 then (if a6 < 892 then (if a7 < 856 then (if a8 < 857 then (if a9 < 820 then (if a10 < 896 then (if a11 < 860 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a13 then 1 else 0 fi
    return verify
