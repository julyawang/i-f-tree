def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, private field a15, private field a16, private field a17, private field a18, private field a19, private field a20, private field a21, private field a22, private field a23, field a25) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 830 then (if a2 < 832 then (if a3 < 831 then (if a4 < 859 then (if a5 < 892 then (if a6 < 854 then (if a7 < 861 then (if a8 < 885 then (if a9 < 873 then (if a10 < 818 then (if a11 < 889 then (if a12 < 872 then (if a13 < 862 then (if a14 < 896 then (if a15 < 888 then (if a16 < 829 then (if a17 < 870 then (if a18 < 892 then (if a19 < 812 then (if a20 < 856 then (if a21 < 819 then (if a22 < 818 then (if a23 < 810 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a25 then 1 else 0 fi
    return verify
