def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, private field a15, private field a16, private field a17, field a19) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 35 then (if a2 < 61 then (if a3 < 89 then (if a4 < 46 then (if a5 < 42 then (if a6 < 96 then (if a7 < 21 then (if a8 < 11 then (if a9 < 13 then (if a10 < 99 then (if a11 < 42 then (if a12 < 83 then (if a13 < 36 then (if a14 < 75 then (if a15 < 16 then (if a16 < 21 then (if a17 < 37 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a19 then 1 else 0 fi
    return verify
