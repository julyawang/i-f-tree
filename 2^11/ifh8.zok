def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, field a9) -> (field): 
    field res = (if a0 >= 1320 then (if a1 < 1801 then (if a2 < 1663 then (if a3 < 1046 then (if a4 < 1763 then (if a5 < 1178 then (if a6 < 1044 then (if a7 < 1352 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a9 then 1 else 0 fi
    return verify
