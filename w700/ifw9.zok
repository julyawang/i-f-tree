def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 625 then 1 else 0 fi) + (if a2 >= 617 then 1 else 0 fi) + (if a3 >= 633 then 1 else 0 fi) + (if a4 >= 613 then 1 else 0 fi) + (if a5 >= 667 then 1 else 0 fi) + (if a6 >= 692 then 1 else 0 fi) + (if a7 >= 658 then 1 else 0 fi) + (if a8 >= 606 then 1 else 0 fi)
    return res