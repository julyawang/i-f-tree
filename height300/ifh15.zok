def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, field a16) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 269 then (if a2 < 263 then (if a3 < 209 then (if a4 < 246 then (if a5 < 273 then (if a6 < 297 then (if a7 < 250 then (if a8 < 249 then (if a9 < 237 then (if a10 < 213 then (if a11 < 272 then (if a12 < 280 then (if a13 < 265 then (if a14 < 298 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a16 then 1 else 0 fi
    return verify
