def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, private field a15, private field a16, private field a17, private field a18, private field a19, private field a20, private field a21, private field a22, private field a23, private field a24, private field a25, private field a26, field a28) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 266 then (if a2 < 274 then (if a3 < 209 then (if a4 < 244 then (if a5 < 253 then (if a6 < 212 then (if a7 < 265 then (if a8 < 267 then (if a9 < 243 then (if a10 < 217 then (if a11 < 281 then (if a12 < 225 then (if a13 < 245 then (if a14 < 262 then (if a15 < 209 then (if a16 < 215 then (if a17 < 272 then (if a18 < 256 then (if a19 < 290 then (if a20 < 256 then (if a21 < 278 then (if a22 < 252 then (if a23 < 217 then (if a24 < 293 then (if a25 < 268 then (if a26 < 200 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a28 then 1 else 0 fi
    return verify
