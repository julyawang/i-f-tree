def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, field a8) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 255 then (if a2 < 247 then (if a3 < 291 then (if a4 < 280 then (if a5 < 294 then (if a6 < 241 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a8 then 1 else 0 fi
    return verify
