def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, private field a15, private field a16, private field a17, private field a18, private field a19, private field a20, field a22) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 212 then (if a2 < 287 then (if a3 < 270 then (if a4 < 285 then (if a5 < 211 then (if a6 < 205 then (if a7 < 208 then (if a8 < 212 then (if a9 < 294 then (if a10 < 210 then (if a11 < 207 then (if a12 < 213 then (if a13 < 251 then (if a14 < 229 then (if a15 < 219 then (if a16 < 297 then (if a17 < 288 then (if a18 < 274 then (if a19 < 241 then (if a20 < 253 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a22 then 1 else 0 fi
    return verify
