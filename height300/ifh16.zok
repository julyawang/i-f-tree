def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, private field a15, field a17) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 208 then (if a2 < 258 then (if a3 < 265 then (if a4 < 293 then (if a5 < 230 then (if a6 < 236 then (if a7 < 200 then (if a8 < 213 then (if a9 < 251 then (if a10 < 268 then (if a11 < 209 then (if a12 < 200 then (if a13 < 266 then (if a14 < 239 then (if a15 < 211 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a17 then 1 else 0 fi
    return verify
