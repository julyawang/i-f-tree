def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, private field a15, private field a16, private field a17, private field a18, private field a19, private field a20, private field a21, private field a22, field a24) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 243 then (if a2 < 226 then (if a3 < 205 then (if a4 < 262 then (if a5 < 206 then (if a6 < 206 then (if a7 < 229 then (if a8 < 270 then (if a9 < 235 then (if a10 < 215 then (if a11 < 207 then (if a12 < 244 then (if a13 < 216 then (if a14 < 278 then (if a15 < 261 then (if a16 < 283 then (if a17 < 274 then (if a18 < 255 then (if a19 < 234 then (if a20 < 259 then (if a21 < 278 then (if a22 < 231 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a24 then 1 else 0 fi
    return verify
