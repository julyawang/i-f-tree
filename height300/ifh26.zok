def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, private field a15, private field a16, private field a17, private field a18, private field a19, private field a20, private field a21, private field a22, private field a23, private field a24, private field a25, field a27) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 242 then (if a2 < 234 then (if a3 < 276 then (if a4 < 211 then (if a5 < 236 then (if a6 < 286 then (if a7 < 237 then (if a8 < 266 then (if a9 < 206 then (if a10 < 202 then (if a11 < 283 then (if a12 < 234 then (if a13 < 201 then (if a14 < 222 then (if a15 < 286 then (if a16 < 290 then (if a17 < 235 then (if a18 < 221 then (if a19 < 231 then (if a20 < 231 then (if a21 < 268 then (if a22 < 219 then (if a23 < 287 then (if a24 < 205 then (if a25 < 207 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a27 then 1 else 0 fi
    return verify
