def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, private field a15, private field a16, private field a17, private field a18, private field a19, private field a20, private field a21, private field a22, private field a23, field a25) -> (field): 
    field res = (if a0 >= 18 then (if a1 < 293 then (if a2 < 212 then (if a3 < 246 then (if a4 < 257 then (if a5 < 258 then (if a6 < 293 then (if a7 < 231 then (if a8 < 253 then (if a9 < 200 then (if a10 < 285 then (if a11 < 218 then (if a12 < 241 then (if a13 < 297 then (if a14 < 273 then (if a15 < 246 then (if a16 < 287 then (if a17 < 268 then (if a18 < 289 then (if a19 < 252 then (if a20 < 263 then (if a21 < 257 then (if a22 < 239 then (if a23 < 234 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a25 then 1 else 0 fi
    return verify
