def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, field a13) -> (field): 
    field res = (if a >= 18 then (if a1 < 10 then (if a2 < 90 then (if a3 < 37 then (if a4 < 35 then (if a5 < 5 then (if a6 < 29 then (if a7 < 79 then (if a8 < 5 then (if a9 < 77 then (if a10 < 13 then (if a11 < 25 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a13 then 1 else 0 fi
    return verify
