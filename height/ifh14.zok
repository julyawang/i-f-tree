def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, field a15) -> (field): 
    field res = (if a >= 18 then (if a1 < 67 then (if a2 < 37 then (if a3 < 65 then (if a4 < 96 then (if a5 < 95 then (if a6 < 24 then (if a7 < 23 then (if a8 < 41 then (if a9 < 43 then (if a10 < 63 then (if a11 < 88 then (if a12 < 68 then (if a13 < 98 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a15 then 1 else 0 fi
    return verify
