def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12, private field a13, private field a14, private field a15, private field a16, private field a17, private field a18, private field a19, private field a20, private field a21, private field a22, private field a23, field a25) -> (field): 
    field res = (if a >= 18 then (if a1 < 83 then (if a2 < 33 then (if a3 < 26 then (if a4 < 17 then (if a5 < 85 then (if a6 < 56 then (if a7 < 72 then (if a8 < 61 then (if a9 < 50 then (if a10 < 98 then (if a11 < 13 then (if a12 < 45 then (if a13 < 91 then (if a14 < 38 then (if a15 < 65 then (if a16 < 97 then (if a17 < 19 then (if a18 < 33 then (if a19 < 94 then (if a20 < 26 then (if a21 < 70 then (if a22 < 19 then (if a23 < 83 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a25 then 1 else 0 fi
    return verify
