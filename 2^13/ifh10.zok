def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, field a11) -> (field): 
    field res = (if a0 >= 7953 then (if a1 < 5656 then (if a2 < 7231 then (if a3 < 5553 then (if a4 < 6289 then (if a5 < 5812 then (if a6 < 6839 then (if a7 < 7226 then (if a8 < 5057 then (if a9 < 4192 then  1 else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a11 then 1 else 0 fi
    return verify
