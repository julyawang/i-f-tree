def main(private field a0, private field a1, private field a2, field a4) -> (field): 
    field res = (if a0 >= 7953 then (if a1 < 4802 then (if a2 < 4919 then  1 else 0 fi) else 0 fi) else 0 fi)
    field verify = if res == a4 then 1 else 0 fi
    return verify
