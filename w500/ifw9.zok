def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 496 then 1 else 0 fi) + (if a2 >= 461 then 1 else 0 fi) + (if a3 >= 461 then 1 else 0 fi) + (if a4 >= 428 then 1 else 0 fi) + (if a5 >= 430 then 1 else 0 fi) + (if a6 >= 402 then 1 else 0 fi) + (if a7 >= 429 then 1 else 0 fi) + (if a8 >= 473 then 1 else 0 fi)
    return res