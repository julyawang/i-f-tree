def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 44 then 1 else 0 fi) + (if a2 >= 16 then 1 else 0 fi) + (if a3 >= 17 then 1 else 0 fi) + (if a4 >= 94 then 1 else 0 fi) + (if a5 >= 27 then 1 else 0 fi) + (if a6 >= 62 then 1 else 0 fi) + (if a7 >= 72 then 1 else 0 fi) + (if a8 >= 65 then 1 else 0 fi) + (if a9 >= 13 then 1 else 0 fi)
    return res