def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 41 then 1 else 0 fi) + (if a2 >= 94 then 1 else 0 fi) + (if a3 >= 53 then 1 else 0 fi) + (if a4 >= 43 then 1 else 0 fi) + (if a5 >= 87 then 1 else 0 fi) + (if a6 >= 1 then 1 else 0 fi) + (if a7 >= 73 then 1 else 0 fi) + (if a8 >= 22 then 1 else 0 fi) + (if a9 >= 81 then 1 else 0 fi) + (if a10 >= 47 then 1 else 0 fi) + (if a11 >= 93 then 1 else 0 fi)
    return res