def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7, private field a8, private field a9, private field a10, private field a11, private field a12) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 8 then 1 else 0 fi) + (if a2 >= 1 then 1 else 0 fi) + (if a3 >= 0 then 1 else 0 fi) + (if a4 >= 30 then 1 else 0 fi) + (if a5 >= 41 then 1 else 0 fi) + (if a6 >= 41 then 1 else 0 fi) + (if a7 >= 2 then 1 else 0 fi) + (if a8 >= 39 then 1 else 0 fi) + (if a9 >= 72 then 1 else 0 fi) + (if a10 >= 39 then 1 else 0 fi) + (if a11 >= 55 then 1 else 0 fi) + (if a12 >= 13 then 1 else 0 fi)
    return res