def main(private field a0, private field a1, private field a2, private field a3, private field a4, private field a5, private field a6, private field a7) -> (field): 
    field res = (if a0 >= 20 then 1 else 0 fi) + (if a1 >= 40 then 1 else 0 fi) + (if a2 >= 14 then 1 else 0 fi) + (if a3 >= 28 then 1 else 0 fi) + (if a4 >= 70 then 1 else 0 fi) + (if a5 >= 80 then 1 else 0 fi) + (if a6 >= 43 then 1 else 0 fi) + (if a7 >= 77 then 1 else 0 fi)
    return res